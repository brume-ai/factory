#!/usr/bin/env bash
# Un jeton DISTINCT de celui que t_setup exporte (FACTORY_TOKEN=t0k3n) : la
# boucle doit frapper le sien en tete de tour, et c est celui-la que l agent
# et le menage doivent voir.
echo frappe-ce-tour
